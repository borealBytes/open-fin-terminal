// Dummy passing test to satisfy Vitest/CI for adapters-oss-shared
import { describe, it, expect } from 'vitest';
describe('dummy', () => {
  it('should pass', () => {
    expect(true).toBe(true);
  });
});
