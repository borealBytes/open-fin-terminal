/**
 * Stooq CSV data adapter for Open Financial Terminal.
 * 
 * Provides historical prices from CSV files (fallback adapter).
 */

export { StooqAdapter } from './stooq.adapter';
export type * from './types';
