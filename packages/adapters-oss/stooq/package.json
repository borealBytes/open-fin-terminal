{
  "name": "@open-fin-terminal/adapters-stooq",
  "version": "0.1.0",
  "private": true,
  "description": "Stooq CSV data adapter for Open Financial Terminal",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "lint": "echo 'Lint configuration pending ESLint 9 flat config migration (Phase 2)'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf dist .turbo node_modules"
  },
  "dependencies": {
    "@open-fin-terminal/adapters": "workspace:*",
    "@open-fin-terminal/shared": "workspace:*",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^22.8.7",
    "@vitest/coverage-v8": "^2.1.4",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
